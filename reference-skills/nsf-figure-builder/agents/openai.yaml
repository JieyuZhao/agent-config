interface:
  display_name: "Figure Prompt Builder"
  short_description: "Turn source text or figure ideas into copy-ready downstream image-model prompts"
  default_prompt: "Use $figure-prompt-builder to decide what figure this section needs, distill the core message into a concise figure brief, and write a prompt pack I can paste into a downstream image model."
